# neural-network-challenge-2
Could not figure out how to do two branched output layers while not using a sequential model.
I am tasked with creating a neural network that HR can use to predict whether employees are likely to leave the
company and if employees may be better suited to other departments.
